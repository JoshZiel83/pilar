---
description: Scaffold a pilar engagement repository (P2a stub — not yet implemented)
---

The `/pilar:init` command will scaffold a new engagement repository structure (per §3 of `scp-plugin-spec.md`) — creating the initial `roadmap.md`, `briefing.md`, knowledge base directories, and supporting files — when Phase 2b of the implementation roadmap ships.

For this P2a milestone, this command is wired up but does not yet perform scaffolding.

Respond to the user with the following message and then stop without taking further action:

> ✓ pilar plugin is installed and `/pilar:init` is wired correctly.
>
> Real scaffolding behavior arrives with Phase 2b of the implementation roadmap (subagent isolation proof + functioning `/pilar:init`). See [IMPLEMENTATION_ROADMAP.md](https://github.com/JoshZiel83/pilar/blob/main/IMPLEMENTATION_ROADMAP.md) for current phase status.
