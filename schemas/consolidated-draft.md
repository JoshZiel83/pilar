---
artifact: consolidated-draft
draft_id: <cd-NNN — kebab-case sequential id per `docs/CONVENTIONS.md`>
project: <project-id>
created: <iso-date>
---

# Consolidated Draft

> Single coherent deliverable view per §6.7 of `scp-plugin-spec.md`. Assembled from `briefing.md` + every approved pillar + `lexicon.md` + `style-guide.md` by `/pilar:consolidate`. The Editor edits this file at `operating_context: consolidated-draft`; the Fact-Checker reads it; the Strategic Reviewer reads it (alongside `briefing.md` and `roadmap.md`). Open `GAP-NNN` and `ASP-NNN` register entries are deliberately excluded — registers are working-state, not deliverable content. The canonical source files are unchanged by consolidation; this is an assembled view.

## Briefing

<assembled extract from `briefing.md` — each `## <section>` from the briefing reproduced here as `### <section>` (demoted one level).>

## Pillars

<every approved pillar's full body assembled in `pillar-id` order, each as a `### Pillar <pillar-id>: <slug>` entry. The pillar body is reproduced verbatim with its native H2 sections demoted: `## Strategic Rationale` → `#### Strategic Rationale`; `### SS-NN` → `##### SS-NN`; `#### RS-NN` → `###### RS-NN`. Original frontmatter is dropped at assembly time.>

## Lexicon

<assembled lexicon entries from `lexicon.md` — each `### <term>` reproduced here as `### <term>`. These are reference for the Editor's harmonization pass at consolidated context; the canonical lexicon file is not modified by consolidation.>

## Style Guide

<assembled style-guide sections from `style-guide.md` — each `## <section>` reproduced here as `### <section>`. These are reference for the Editor's pass; the canonical style-guide file is not modified by consolidation.>
